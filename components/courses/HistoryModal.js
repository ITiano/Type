import ModalContainer from "components/utils/ModalContainer";
import React from "react";

const HistoryModal = ({ open, setOpen }) => {
  return (
    <ModalContainer open={open} setOpen={setOpen} width="600px" clear >
      salam
    </ModalContainer>
  );
};

export default HistoryModal;
